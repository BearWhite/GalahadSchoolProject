{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pret list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Datereservation</th>
                <th>Isreservation</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('pret_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.dateReservation %}{{ entity.dateReservation|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.isReservation }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('pret_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('pret_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('pret_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
